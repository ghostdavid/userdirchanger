# Windows 11 用户核心文件夹安全迁移脚本 (C盘转D盘)

## 📌 简介
本脚本旨在安全、无损地将 Windows 11 用户的核心数据文件夹（包括桌面、文档、下载、图片、音乐、视频）从系统盘 (C:) 迁移至数据盘 (D:)。

相比于传统的剪切/粘贴或修改注册表，本脚本进行了深度安全加固，解决了文件被占用导致的数据丢失、自定义图标失效以及权限阻断等常见痛点，确保迁移过程稳定可靠。

## ✨ 核心特性
- **防断档安全机制**：使用 Robocopy 进行底层迁移。如果遇到文件被锁定（如正在编辑的文档），脚本会自动中止修改注册表，绝不让系统路径指向一个半成品文件夹。
- **图标自动修复**：自动恢复目标文件夹的 `ReadOnly` 属性，确保迁移后 D 盘的文件夹依然保留系统原生的定制图标（如桌面、下载的特殊图标）。
- **多线程加速**：开启多线程传输，海量小文件（如前端项目、碎图片）迁移速度大幅提升。
- **自动防呆**：强制校验管理员权限，遇到已迁移过的文件夹会自动跳过。

## 🚀 使用指南

### 第一步：准备工作（非常重要！）
1. **关闭占用程序**：退出所有日常软件，尤其是浏览器（会占用下载文件夹）、微信/QQ（会占用文档文件夹）以及 Office 软件。
2. **退出 OneDrive**：如果你的系统开启了 OneDrive 文件夹备份，请在右下角任务栏右键 OneDrive 图标，**暂停同步**或在设置中**停止备份**这几个核心文件夹。

### 第二步：执行脚本
1. 将脚本代码保存为 `userdirchanger.ps1`（务必以**带有 BOM 的 UTF-8** 编码保存）。
2. 点击 Windows 开始菜单，搜索 `PowerShell`。
3. 选择 **“以管理员身份运行”**。
4. 在命令行中输入脚本所在路径并回车运行（例如：`C:\Users\Admin\Desktop\userdirchanger.ps1`）。
5. 脚本运行结束后，Windows 资源管理器会自动重启，你的桌面会闪烁一下，此时迁移即刻生效。

---

## ❓ 常见问题解答 (Q&A)

### Q1: 运行脚本时红字报错“在此系统上禁止运行脚本”，怎么办？
**A:** 这是 Windows 默认的安全策略。请在管理员 PowerShell 窗口中运行以下命令来解除限制：
`Set-ExecutionPolicy RemoteSigned`
输入 `Y` 确认后，再次运行本脚本即可。

### Q2: 脚本提示“迁移发生严重错误 (退出码: 8)”，这是怎么回事？
**A:** 这说明原文件夹（C盘）里有文件正在被其他程序死死咬住（占用），导致无法移动。
**解决办法：** 脚本已经自动保护了你的数据并停止了该文件夹的迁移。请检查是否有后台程序没关（如百度网盘、各类游戏启动器），重启电脑后再运行一次脚本即可。

### Q3: 迁移完成后，C盘原来的文件夹还在吗？
**A:** 脚本默认会在移动成功后删除原文件夹。但是，如果某些空文件夹的外壳被系统进程临时锁定，脚本会保留这个空壳以防崩溃。由于里面的文件已经全部转移，这个空壳不会占用空间，你可以手动删除它，或者不用管它。

### Q4: 迁移后，D 盘文件夹的图标变成了普通的黄色文件夹，怎么办？
**A:** 脚本内置了图标修复功能。如果依然是普通图标，通常是因为原文件夹的 `desktop.ini` 文件被你误删过。你可以右键该文件夹 -> `属性` -> `自定义` -> `更改图标`，手动选回系统图标，系统会自动为你生成一个新的 `desktop.ini`。

### Q5: 我后悔了，想把这些文件夹改回 C 盘，怎么还原？
**A:** 打开 D 盘迁移后的文件夹，右键需要还原的文件夹（如“桌面”） -> `属性` -> `位置` 选项卡，点击 **“还原默认值”**，然后点击“确定”，并在弹出的“是否移动所有文件”提示中选择“是”即可。
